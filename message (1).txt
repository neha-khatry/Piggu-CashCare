import 'package:flutter/material.dart';
import 'package:firebase_auth/firebase_auth.dart';
import 'LoginPage.dart';
import 'ProfilePage.dart';
import 'GoalsPage.dart';
import 'AddIncomePage.dart';
import 'AddExpensePage.dart';

class HomePage extends StatefulWidget {
  final User user;

  HomePage({required this.user});

  @override
  _HomePageState createState() => _HomePageState();
}

class _HomePageState extends State<HomePage> {
  Future<void> _logout(BuildContext context) async {
    await FirebaseAuth.instance.signOut();
    Navigator.pushReplacement(
      context,
      MaterialPageRoute(builder: (context) => LoginPage()),
    );
  }

  void _navigateToNotificationsPage(BuildContext context) {
    // TODO: Implement navigation to NotificationsPage
  }

  void _navigateToHistoryPage(BuildContext context) {
    // TODO: Implement navigation to HistoryPage
  }

  void _showProfileOptions(BuildContext context) {
    showMenu(
      context: context,
      position: RelativeRect.fromLTRB(0, 50, 0, 0),
      items: [
        PopupMenuItem(
          child: Text('Personal Details'),
          value: 'personal_details',
        ),
        PopupMenuItem(
          child: Text('Balance'),
          value: 'balance',
        ),
        PopupMenuItem(
          child: Text('Total Income'),
          value: 'total_income',
        ),
        PopupMenuItem(
          child: Text('Expense Categories'),
          value: 'expense_categories',
        ),
        PopupMenuItem(
          child: Text('FAQs'),
          value: 'faqs',
        ),
        PopupMenuItem(
          child: Text('Log out'),
          value: 'logout',
        ),
      ],
      elevation: 8.0,
    ).then<void>((String? value) {
      if (value != null) {
        switch (value) {
          case 'personal_details':
          // Handle Personal Details option press
            break;
          case 'balance':
          // Handle Balance option press
            break;
          case 'total_income':
          // Handle Total Income option press
            break;
          case 'expense_categories':
          // Handle Expense Categories option press
            break;
          case 'faqs':
          // Handle FAQs option press
            break;
          case 'logout':
            _logout(context);
            break;
        }
      }
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Dashboard'),
      ),
      body: Column(
        children: [
          Padding(
            padding: const EdgeInsets.all(16.0),
            child: Column(
              children: [
                Row(
                  mainAxisAlignment: MainAxisAlignment.spaceEvenly,
                  children: [
                    ElevatedButton(
                      onPressed: () {
                        showMenu(
                          context: context,
                          position: RelativeRect.fromLTRB(0, 50, 0, 0),
                          items: [
                            PopupMenuItem(
                              child: Text('Add Income'),
                              value: 'add_income',
                            ),
                          ],
                        ).then<void>((String? value) {
                          if (value != null) {
                            if (value == 'add_income') {
                              Navigator.push(
                                context,
                                MaterialPageRoute(builder: (context) => AddIncomePage()),
                              );
                            }
                          }
                        });
                      },
                      child: Text('Income'),
                    ),
                    ElevatedButton(
                      onPressed: () {
                        showMenu(
                          context: context,
                          position: RelativeRect.fromLTRB(0, 50, 0, 0),
                          items: [
                            PopupMenuItem(
                              child: Text('Add Expense'),
                              value: 'add_expense',
                            ),
                          ],
                        ).then<void>((String? value) {
                          if (value != null) {
                            if (value == 'add_expense') {
                              Navigator.push(
                                context,
                                MaterialPageRoute(builder: (context) => AddExpensePage()),
                              );
                            }
                          }
                        });
                      },
                      child: Text('Expenditure'),
                    ),
                  ],
                ),
                SizedBox(height: 16.0),
                Row(
                  mainAxisAlignment: MainAxisAlignment.spaceEvenly,
                  children: [
                    ElevatedButton(
                      onPressed: () {
                        Navigator.push(
                          context,
                          MaterialPageRoute(builder: (context) => GoalSettingPage(user: widget.user)),
                        );
                      },
                      child: Text('Goals'),
                    ),
                    ElevatedButton(
                      onPressed: () {
                        // Handle Scan Receipt button press
                      },
                      child: Text('Scan Receipt'),
                    ),
                  ],
                ),
              ],
            ),
          ),
          Expanded(
            child: Center(
              child: Column(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  // Remove the Text widget that displays the name
                ],
              ),
            ),
          ),
        ],
      ),
      bottomNavigationBar: BottomAppBar(
        child: Row(
          mainAxisAlignment: MainAxisAlignment.spaceEvenly,
          children: [
            IconButton(
              icon: Icon(Icons.home),
              onPressed: () {
                // Handle Dashboard button press
              },
            ),
            IconButton(
              icon: Icon(Icons.history),
              onPressed: () {
                _navigateToHistoryPage(context);
              },
            ),
            IconButton(
              icon: Icon(Icons.notifications),
              onPressed: () {
                _navigateToNotificationsPage(context);
              },
            ),
            PopupMenuButton(
              icon: Icon(Icons.account_circle),
              onSelected: (String value) {
                switch (value) {
                  case 'personal_details':
                  // Handle Personal Details option press
                    break;
                  case 'balance':
                  // Handle Balance option press
                    break;
                  case 'total_income':
                  // Handle Total Income option press
                    break;
                  case 'expense_categories':
                  // Handle Expense Categories option press
                    break;
                  case 'faqs':
                  // Handle FAQs option press
                    break;
                  case 'logout':
                    _logout(context);
                    break;
                }
              },
              itemBuilder: (BuildContext context) => <PopupMenuEntry<String>>[
                const PopupMenuItem<String>(
                  value: 'personal_details',
                  child: Text('Personal Details'),
                ),
                const PopupMenuItem<String>(
                  value: 'balance',
                  child: Text('Balance'),
                ),
                const PopupMenuItem<String>(
                  value: 'total_income',
                  child: Text('Total Income'),
                ),
                const PopupMenuItem<String>(
                  value: 'expense_categories',
                  child: Text('Expense Categories'),
                ),
                const PopupMenuItem<String>(
                  value: 'faqs',
                  child: Text('FAQs'),
                ),
                const PopupMenuItem<String>(
                  value: 'logout',
                  child: Text('Log out'),
                ),
              ],
            ),
          ],
        ),
      ),
    );
  }
}
